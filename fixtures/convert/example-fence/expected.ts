/**
 * Builds a config object from options.
 * @param options - The raw options.
 * @example
 * ```typescript
 * buildConfig({ debug: true })
 * ```
 */
export function buildConfig(options: object): object {
  return options;
}
